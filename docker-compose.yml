
version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - VITE_ADMIN_USERNAME=admin
      - VITE_ADMIN_PASSWORD=admin
    volumes:
      - ./data:/app/data
    restart: unless-stopped
